export type ChildrenProps = {
 children: React.ReactNode
}

export type SectionProps = {
 cls: string
 children: React.ReactNode
}

export type HeadingProps = {
 cls: string
 sub: string
 title: string
}
